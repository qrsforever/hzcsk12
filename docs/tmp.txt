https://releases.hashicorp.com/consul/
docker run -d --network host -v /data/consul:/consul/data -e CONSUL_BIND_INTERFACE='eth0' --name=k12ai_consul consul agent -server -bootstrap -ui -client='0.0.0.0'

# -bootstrap-expect=2 

docker run -dit \
           --restart=always \
           --name=gamma-consul \
           --volume /data/:/data \
           --network host \
           --hostname gamma-consul \
           consul agent -dev -server -bootstrap \
           -node=gamma \
           -data-dir=/data/consul \
           -datacenter=gamma -client='0.0.0.0' -ui

# http://gamma:8500/v1/catalog/deregister/k12ai
